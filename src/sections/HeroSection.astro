---
import { loadContent } from "@yysng/astro-boilerplate";
import HeroView from "./HeroView.astro";

const { data } = Astro.props;
const content = data ?? await loadContent("hero", Astro.locals.runtime.env);
---

<HeroView data={content} />